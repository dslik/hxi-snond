/*
## Configuration file verification

Validates the JSON-formatted configuration file
*/

    #include "cJSON.h"     // cJSON JSON parsing library

    int32_t hxi_config_json_validate(cJSON* config_json);

